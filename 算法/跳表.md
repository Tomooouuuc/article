```c++
#include<iostream>
#include<vector>
#include<algorithm>
#include<random>
#include<chrono>
using namespace std;

struct Node{
	int key,level;
	Node *next,*up,*down;
};

struct Skiplist{
	Node *head,*tail;
	int max_level;
};

Node* getNewNode(int key,int n){
	Node *nodes=(Node*)malloc(sizeof(Node)*n);
	for(int i=0;i<n;i++){
		nodes[i].key=key;
		nodes[i].level=i;
		nodes[i].next=NULL;
		nodes[i].down=(i?nodes+(i-1):NULL);
		nodes[i].up=(i+1<n?nodes+(i+1):NULL);
	}
	return nodes+n-1;
}

Skiplist* getNewSkiplist(int n){
	Skiplist *s=(Skiplist*)malloc(sizeof(Skiplist));
	s->head=getNewNode(INT_MIN,n);
	s->tail=getNewNode(INT_MAX,n);
	s->max_level=n;
	Node *p=s->head,*q=s->tail;
	while(p){
		p->next=q;
		p=p->down;
		q=q->down;
	}
	return s;
}

Node* find(Skiplist *s,int x){
	Node *p=s->head;
	while(p&&p->key!=x){
		if(p->next->key<=x){
			p=p->next;
		}else{
			p=p->down;
		}
	}
	return p;
}

double randDouble(){
	int MAX_RAND_N=1000000;
	mt19937 gen(random_device{}());
	uniform_int_distribution dist(0,MAX_RAND_N);
	return (dist(gen)%MAX_RAND_N)*1.0/MAX_RAND_N;
}

int randLevel(Skiplist *s){
	int level=1;
	double p=1.0/2.0;
	while(randDouble()<p){
		level++;
	}
	return min(s->max_level,level);
}

void insert(Skiplist *s,int x){
	int level=randLevel(s);
	cout<<"key:"<<x<<" level:"<<level<<endl;
	Node *node=getNewNode(x,level);
	Node *p=s->head;
	while(p->level!=node->level){
		p=p->down;
	}
	while(p){
		while(p->next->key<node->key){
			p=p->next;
		}
		node->next=p->next;
		p->next=node;
		p=p->down;
		node=node->down;
	}
	return;
}

void clearNode(Node *p){
	if(p==NULL){
		return;
	}
	free(p);
	return;
}

void clearSkiplist(Skiplist *s){
	Node *p=s->head,*q;
	while(p->level!=0){
		p=p->down;
	}
	while(p){
		q=p->next;
		clearNode(p);
		p=q;
	}
}

void deleteNode(Skiplist *s,int x){
	Node *p=s->head,*q;
	while(p){
		if(p->next->key>x){
			p=p->down;
		}else if(p->next->key<x){
			p=p->next;
		}else{
			q=p->next;
			p->next=p->next->next;
			p=p->down;
		}
	}
	cout<<"节点level: "<<q->level<<"  key:"<<x<<endl;
	clearNode(q);
}

ostream &operator<<(ostream &out,Skiplist *s){
	Node *p=s->head;
	while(p->down){
		p=p->down;
	}
	while(p->next){
		out<<p->key<<" ";
		p=p->next;
	}
	return out;
}

void output(Skiplist *s){
	Node *p=s->head;
	printf("\n");
	while(p->level>0){
		p=p->down;
	}
	while(p){
		bool flag=(p->key!=INT_MIN&&p->key!=INT_MAX);
		for(Node *q=p;flag&&q;q=q->up){
			printf("%4d",q->key);
		}
		if(flag) {
			printf("\n");
		}
		p=p->next;
	}
	printf("===============\n");
	return;
}

int main(){
	Skiplist *s=getNewSkiplist(32);
	static random_device rd;
	static mt19937 gen(rd());
	uniform_int_distribution dist(0, 100);
	vector<int> res{5,2,7,3,8,11,65};
	for(size_t i=0;i<res.size();i++){
		insert(s,res[i]);
		output(s);
	}
	
	cout<<"开始删除"<<endl;
	vector<int> bbb{2,65,7};
	for(int i=0;i<3;i++){
		deleteNode(s,bbb[i]);
		output(s);
	}
	
	vector<int> aaa{5,65,8,9};
	for(size_t i=0;i<aaa.size();i++){
		Node *p=find(s,aaa[i]);
		if(p){
			cout<<"find:key:"<<p->key<<"  level:"<<p->level<<endl;
		}else{
			cout<<"find:key:"<<aaa[i]<<"  NULL"<<endl;
		}
	}
	clearSkiplist(s);
	return 0;
}
```

